/**
 * Centralized export for all test fixtures
 * 
 * Import all fixtures from a single location for convenience.
 * 
 * Usage:
 * import { mockBasicPose, mockStudent, mockGameSession } from '../fixtures';
 */

// Re-export all pose fixtures
export * from './mockPoseData';

// Re-export all user fixtures
export * from './mockUserData';

// Re-export all game fixtures
export * from './mockGameData';
